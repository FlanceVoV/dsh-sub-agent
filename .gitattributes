# 统一换行符：仓库内一律 LF，检出到工作区也一律 LF。
#
# 为什么必须有这个文件：`lib/client.js` 是 `lib/parts/*.js` 按序拼接的**产物**，
# `scripts/build-client.mjs --check` 与 `scripts/pack.mjs` 做的是**逐字节**比对
# （那是刻意的：分片是唯一真相来源，比对必须严格，不能「差不多就算过」）。
#
# 而 Windows 上常见的 `core.autocrlf=true` 会在检出时把 LF 改写成 CRLF，
# 于是分片（CRLF）与产物（CRLF）虽然内容相同，却与仓库里存的 LF 版本对不上：
# 克隆下来第一次跑 `--check` 或打包就会失败，报「产物与分片不一致」——
# 一条把人指向错误方向的报错（文件其实没问题，是换行符被改了）。
#
# 声明 `eol=lf` 后，仓库里存的、检出的都是 LF，跨平台一致。
# git 仍会自动识别二进制文件（png 等）不做转换。
* text=auto eol=lf
